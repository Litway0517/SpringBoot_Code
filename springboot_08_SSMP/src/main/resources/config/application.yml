# 更改端口
server:
  port: 8083

## druid数据源
#spring:
#  datasource:
#    druid:
#      driver-class-name: com.mysql.cj.jdbc.Driver
#      # url: jdbc:mysql://localhost:3306/springboot_code
#      url: jdbc:mysql://192.168.137.1:3306/springboot_code
#      username: root
#      password: root
#
## 表名前缀
#mybatis-plus:
#  global-config:
#    db-config:
#      table-prefix: t_
#      id-type: auto
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl



# SpringBoot的四级配置, 一级最高, 会覆盖二三四级
# 4- 类路径下的application.yml: classpath:/application.yml
# 3- 类路径下的config目录下的application.yml: classpath:/config/application.yml
# 2- jar包所在目录同级中的: jar同级-application.yml
# 1- jar包所在目录下的config目录中的application.yml: jar-config/application.yml
