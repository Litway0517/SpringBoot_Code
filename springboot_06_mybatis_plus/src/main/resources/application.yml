# 配置数据源
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot_code?serverTimeZone=UTC
    username: root
    password: root

# 配置前缀
mybatis-plus:
  global-config:
    db-config:
      table-prefix: t_

